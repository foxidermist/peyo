# peyo

Чисто питоновский порт **eyo-kernel** (восстановление буквы «ё» в русском тексте по словарю).  
**Node.js не нужен.**

🇬🇧 English documentation: [README.md](README.md)

## Установка

```bash
pip install peyo
```

## Быстрый старт

```python
import peyo

text = "«Лед тронулся, господа присяжные заседатели!»"
print(peyo.yoify(text))
# -> «Лёд тронулся, господа присяжные заседатели!»
```

## Режимы

Ёфикация в русском языке часто неоднозначна:  
`все/всё`, `осел/осёл`, `передохнем/передохнём` и т.д.

Поэтому в библиотеке есть два режима:

- `mode="safe"` (по умолчанию): очень осторожный режим, почти без ошибок
- `mode="not_safe"`: ставит «ё» чаще, но возможны ошибки

```python
import peyo

print(peyo.yoify("Ежик и елка"))
# -> "Ёжик и ёлка"

print(peyo.yoify("все", mode="safe"))
# -> "все"   (safe не лезет в неоднозначные случаи)

print(peyo.yoify("все", mode="not_safe"))
# -> "всё"   (более агрессивно)
```

## API

### `peyo.yoify(text: str, mode: str = "safe") -> str`
Главная функция. Это **стабильный публичный API**.

Алиасы (то же самое):
- `peyo.restore(...)`
- `peyo.yo(...)`

### `peyo.lint(text: str, mode: str = "safe", group_by_words: bool = False) -> list[dict]`
Возвращает список потенциальных замен (текст не меняет).

Пример элемента:
```json
{
  "before": "Лед",
  "after": "Лёд",
  "position": [{"line": 1, "column": 2, "index": 1}]
}
```

## Замечания

- Библиотека словарная (без ML и без настоящего учёта контекста).
- Для полностью автоматической обработки лучше использовать `safe`.
- Для максимального качества обычно делают так:
  1) прогоняют `safe` восстановление
  2) затем смотрят кандидаты `not_safe` (через `lint`) и решают вручную/правилами

## Лицензия и авторство

Проект является Python-портом **eyo-kernel** (автор Denis Seleznev) и использует исходные словари.  
Upstream лицензирован под MIT, см. файл `LICENSE`.
